# The `PSUAN collective` blog

Simple Jekyll-based page collection for the PSUAN collective.

## Building and serving

### Building

Run `bundler exec jekyll build` in the project directory.

### Serving

Run `bundler exec jekyll serve`.

For the live-reload feature add the `-l` flag.
